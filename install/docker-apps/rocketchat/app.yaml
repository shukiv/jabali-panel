slug: rocketchat
popularity: 68
name: Rocket.Chat
tags: ["Communication"]
version: "8.7.1"
description: |
  Self-hosted team chat / collaboration platform — channels, DMs, voice/
  video, file sharing, and integrations. Two services: the Rocket.Chat
  Node.js app and a MongoDB (single-node replica set, required by
  Rocket.Chat). An admin account is pre-created at first boot.
icon: icon.svg
upstream: https://github.com/RocketChat/Rocket.Chat
documentation: https://docs.rocket.chat/

image_channel: rocketchat/rocket.chat:8.7.1@sha256:24aa079410837769e4e36ad275070a9c798f8db59b40ee03e2df3a6425e56eca
update_mode: manual

resources:
  cpu: "2.0"
  memory: "2g"
  pids: 800

volumes:
  - name: uploads
    container_path: /app/uploads
  - name: mongo
    container_path: /data/db

ports:
  - name: http
    container_port: 3000
    protocol: tcp
    default_enabled: true
    default_bind: loopback
    default_reverse_proxy: true
    health_path: /api/info

env:
  - name: ROCKETCHAT_ADMIN_USERNAME
    value: "jabali"
  - name: ROCKETCHAT_ADMIN_PASSWORD
    secret: true
    generate: password32
  - name: ROCKETCHAT_ADMIN_EMAIL
    value: "admin@example.com"

# Operator-supplied SMTP (GH #322): auto-injects standard SMTP_* install fields;
# compose maps them to this app's SMTP env.
smtp: true
